# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# debug reports posted by devices on the LAN (tools/report-server.js)
/.kobin-reports

# production
/build

# deploy cache
/.firebase

# editors & local tooling
/.idea
/.claude
.DS_Store

# env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# one-off jest --json timing dumps (tools/.cache/, moved off the repo root 2026-09-07)
/tools/.cache

# the erase worker bundle: esbuild output, rebuilt by prestart/prebuild (npm run build:worker)
/public/erase-worker.js
